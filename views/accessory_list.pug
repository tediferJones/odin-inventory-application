extends layout 

block content 
  h1= title 

  ul 
    each accessory in accessory_list 
      li 
        a(href=accessory.url) #{accessory.name}

    else 
      li There are no accessories available.